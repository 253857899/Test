<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  
  <mapper namespace="dao.TypeDao">  <!-- xml 与  接口dao关联 -->
  <select id="select" resultType="model.Type">
  select * from type
  </select>
  
  </mapper>